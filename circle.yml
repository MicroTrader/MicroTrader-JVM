machine:
  java:
    version: openjdk8

dependencies:
  override:
    - gradle dependencies --stacktrace --info --debug --no-daemon

test:
  override:
    - gradle test --stacktrace --info --debug --no-daemon