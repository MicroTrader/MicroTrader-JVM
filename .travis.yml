language: java

jdk:
  - oraclejdk8

os:
  - linux
  - osx

env:
  - ARCH=x86_64
  - ARCH=i686

before_install:
  - pip install --user codecov

script:
  - ./gradlew build  --info --no-daemon --stacktrace

after_success:
  - codecov