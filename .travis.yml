language: java

jdk:
  - oraclejdk8

os:
  - linux
  - osx

env:
  - ARCH=x86_64
  - ARCH=i686

before_install:
  - pip install --user codecov

script:
  - ./gradlew build

notifications:
  email:
    - commits@microtrader.groups.io

after_success:
  - codecov